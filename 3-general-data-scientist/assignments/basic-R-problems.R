###########################################
# Assignments for R - Basic 
# complete the problems below on this script
# unless specified otherwise ;) 
###########################################

# 0. Run the following code using the keyboard
# short cut. Why did you get an error? How 
# can you fix it?

for(i in 1:10){
  x <- x + 1 
}

# 1. create a function that rolls a pair of 
# 6 sided die and returns the sum

# 2. put the function from part 1 in a separate 
# script and use the replicate function to roll the 
# die 10,000 times. Save the results to an object and 
# plot the histogram using base R. Calculate the 
# summary statistics of the results.

# 3. install the tidyverse and load ggplot2. Plot the results
# from part 2 using qplot. Load the mpg dataset into the R session
# using the data() function. Calculate the mean city and hwy mileage.
# plot the city miles against the cylinders (cyl).

# 4. using the install.packages function install the following 
# packages at the same time: broom, shiny, and leaflet
